{{- if .Values.secrets }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "gati.fullname" . }}-secret
  labels:
    {{- include "gati.labels" . | nindent 4 }}
type: Opaque
stringData:
  {{- range $key, $value := .Values.secrets }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
{{- end }}
