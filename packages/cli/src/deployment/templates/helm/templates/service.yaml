apiVersion: v1
kind: Service
metadata:
  name: {{ include "gati.fullname" . }}
  labels:
    {{- include "gati.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
  - port: {{ .Values.service.port }}
    targetPort: http
    protocol: TCP
    name: http
  selector:
    {{- include "gati.selectorLabels" . | nindent 4 }}
