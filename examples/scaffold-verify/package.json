{
  "name": "scaffold-verify",
  "version": "0.1.3",
  "private": true,
  "description": "Scaffold verification app",
  "scripts": {
    "dev": "echo 'dev command requires @gati/cli; use tsx or node once available' && exit 0",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@gati-framework/core": "^0.4.0"
  }
}
