/**
 * Ultra-minimal Gati config - everything is optional
 */

export default {
  // Optional: Only needed if you want to override defaults
  // server: { port: 3000 }, // Default: auto-detect available port
  
  // Optional: Only needed for custom overrides
  // routes: {
  //   '/custom': { handler: myHandler, method: 'POST' }
  // },
  
  // Optional: Only needed for global modules
  // modules: {
  //   db: () => createDatabase()
  // }
};